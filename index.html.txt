<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>پنل کاربری</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    
    <style>
        /* استفاده از متغیرهای رنگی تلگرام برای هماهنگی با تم کاربر */
        body {
            background-color: var(--tg-theme-bg-color, #fff);
            color: var(--tg-theme-text-color, #000);
            font-family: Tahoma, sans-serif;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .user-card {
            background-color: var(--tg-theme-secondary-bg-color, #f0f0f0);
            padding: 15px;
            border-radius: 12px;
            width: 100%;
            max-width: 300px;
            text-align: center;
            margin-bottom: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        /* گرید دکمه‌ها */
        .menu-grid {
            display: grid;
            grid-template-columns: 1fr 1fr; /* دو ستون */
            gap: 15px;
            width: 100%;
            max-width: 320px;
        }

        /* استایل دکمه‌ها */
        .menu-btn {
            background-color: var(--tg-theme-button-color, #2481cc);
            color: var(--tg-theme-button-text-color, #fff);
            border: none;
            padding: 15px;
            border-radius: 10px;
            font-size: 14px;
            cursor: pointer;
            transition: opacity 0.2s;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 5px;
        }

        .menu-btn:active {
            opacity: 0.8;
        }

        /* دکمه بزرگ پایین (تمام عرض) */
        .full-width {
            grid-column: span 2;
            background-color: var(--tg-theme-secondary-bg-color, #e0e0e0);
            color: var(--tg-theme-text-color, #000);
            border: 1px solid var(--tg-theme-hint-color, #ccc);
        }

        /* آیکون‌ها (نمادین) */
        .icon { font-size: 20px; }
    </style>
</head>
<body>

    <div class="header">
        <h2>خدمات مالی من</h2>
    </div>

    <div class="user-card">
        <div id="username">در حال بارگذاری...</div>
        <small style="color: var(--tg-theme-hint-color);">خوش آمدید</small>
    </div>

    <div class="menu-grid">
        <button class="menu-btn" onclick="showAlert('بخش کیف پول')">
            <span class="icon">💰</span>
            <span>کیف پول</span>
        </button>
        
        <button class="menu-btn" onclick="showAlert('بخش تراکنش‌ها')">
            <span class="icon">📊</span>
            <span>تراکنش‌ها</span>
        </button>
        
        <button class="menu-btn" onclick="showAlert('بخش تبدیل ارز')">
            <span class="icon">💱</span>
            <span>تبدیل ارز</span>
        </button>
        
        <button class="menu-btn" onclick="showAlert('بخش پشتیبانی')">
            <span class="icon">🎧</span>
            <span>پشتیبانی</span>
        </button>

        <button class="menu-btn full-width" onclick="closeApp()">
            <span class="icon">❌</span>
            <span>خروج از برنامه</span>
        </button>
    </div>

    <script>
        // اتصال به آبجکت تلگرام
        let tg = window.Telegram.WebApp;

        // تمام صفحه کردن اپلیکیشن
        tg.expand();

        // نمایش نام کاربر
        if (tg.initDataUnsafe && tg.initDataUnsafe.user) {
            document.getElementById("username").innerText = 
                tg.initDataUnsafe.user.first_name + " " + (tg.initDataUnsafe.user.last_name || "");
        } else {
            document.getElementById("username").innerText = "کاربر مهمان";
        }

        // عملکرد دکمه‌ها
        function showAlert(sectionName) {
            // در حالت واقعی اینجا درخواست به سرور ارسال می‌شود
            tg.showPopup({
                title: "انتخاب گزینه",
                message: "شما وارد " + sectionName + " شدید.",
                buttons: [{type: "ok"}]
            });
        }

        function closeApp() {
            tg.close();
        }
    </script>
</body>
</html>